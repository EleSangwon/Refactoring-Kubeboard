kind: Service
apiVersion: v1
metadata:
  name: {{ .Values.logreport.service.name }}
 # namespace: {{ .Values.frontendns }}
spec:
  selector:
    app: {{ .Values.labels.name }}
  ports:
    - protocol: "TCP"
      port: 80
      targetPort: 80
  type: ClusterIP