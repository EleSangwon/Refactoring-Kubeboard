{{- define "kubeboard.deployment.tpl" }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kubeboard.fullname" . }}
  labels:
    {{- include "kubeboard.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "kubeboard.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "kubeboard.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "kubeboard.serviceAccountName" . }}
      volumes:
      - name: {{ .Values.volume.pvname }}
        persistentVolumeClaim:
          claimName: {{ .Values.volume.pvcname }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
          - name: {{ .Values.volume.pvname }}
            mountPath: {{ .Values.volume.mountpath }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}

{{- define "kubeboard.deployment" -}}
{{- include "kubeboard.util.merge" (append . "kubeboard.deployment.tpl") -}}
{{- end -}}
