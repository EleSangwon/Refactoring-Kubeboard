{{- define "kubeboard.mainfeservice" -}}
metadata:
  name: {{ include "kubeboard.fullname" . }}-main-fe-svc
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
{{- end -}}
{{- include "kubeboard.service" (list . "kubeboard.mainfeservice" ) }}
